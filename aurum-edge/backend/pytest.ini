[pytest]
asyncio_mode = auto
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
markers =
    live: needs a real network connection to Bybit (skipped unless AURUM_LIVE_TESTS=1)
    slow: takes tens of seconds (a full engine run on the simulated feed)
