/**
 * Intentionally empty.
 *
 * This project's CSS is hand-written and needs no plugins. The file exists so
 * that PostCSS resolution stops here instead of walking up into the parent
 * repository's Tailwind configuration.
 */
const config = { plugins: {} };

export default config;
